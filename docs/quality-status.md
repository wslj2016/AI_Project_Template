# Quality Status

## Template Exception

当前项目是模板项目，不是实际业务项目。

以下 Warning 允许存在：

1. `language_profile=TBD`

   原因：模板支持多个语言。

2. `build skipped`

   原因：没有绑定具体项目。

3. `test skipped`

   原因：没有测试目标。

## Real Project Requirement

当模板复制为真实项目后：

以上 Warning 必须根据实际项目消除。
