# VI 分层架构

## 分层

1. UI 层：主界面、报表。
2. 执行层：测试序列、步骤调度。
3. 业务层：测试逻辑、判据。
4. 驱动层：仪器驱动封装。
5. 硬件抽象层：DUT/仪器统一接口。

## 规则

- 上层只能调用相邻下层。
- 步骤 VI 只接收参数并返回结果，不直接操作仪器。
- 新步骤通过模板 VI 复制创建。

## 数据流

测试参数 → 步骤执行 → 结果采集 → 判据 → 报表
